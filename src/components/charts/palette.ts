/**
 * สีสำหรับกราฟ
 *
 * ⚠️ ตรวจแล้วด้วยเครื่องมือ ไม่ได้เลือกด้วยสายตา
 *
 * ผ่านเกณฑ์ทั้งช่วงความสว่าง ความอิ่มสี และการแยกออกจากกัน "ทุกคู่"
 * ไม่ใช่แค่คู่ที่อยู่ติดกัน — สำหรับผู้ที่มองสีต่างไป ΔE 9.0
 * และสำหรับสายตาปกติ ΔE 19.6
 *
 * ที่ต้องตรวจทุกคู่เพราะกราฟวงกลมทำให้ทุกชิ้นอยู่ติดกันได้หมด ไม่เหมือน
 * กราฟแท่งที่เรียงเป็นแถวและเทียบกันเฉพาะคู่ที่ติดกันจริง ๆ
 *
 * ⚠️ ต้องมีตัวเลขกำกับทุกแท่งและทุกชิ้นเสมอ
 * สีเขียวน้ำทะเลกับสีส้มมีค่าความต่างจากพื้นขาวต่ำกว่า 3:1
 * ตัวเลขที่เห็นชัดคือสิ่งที่ทำให้อ่านค่าได้โดยไม่ต้องพึ่งสี — ห้ามตัดออก
 *
 * ⚠️ เรียงตามลำดับตายตัว ห้ามวนซ้ำ
 * สีผูกกับ "กลุ่มที่เท่าไร" ไม่ใช่ "อันดับที่เท่าไร" — ถ้ากรองแล้วเหลือสามกลุ่ม
 * สีของแต่ละกลุ่มต้องไม่เปลี่ยน ไม่งั้นคนอ่านจะเข้าใจว่าเป็นคนละกลุ่มกัน
 */
export const SERIES = [
  "#2a78d6", // 1 น้ำเงิน
  "#e0782c", // 2 ส้ม
  "#1baf7a", // 3 เขียวน้ำทะเล
  "#c0559e", // 4 บานเย็น
] as const;

/** สีเดียวสำหรับกราฟที่มีชุดข้อมูลเดียว เช่น จำนวนเคสรายเดือน */
export const SINGLE = SERIES[0];

/** เส้นกริดและแกน — ต้องจางกว่าตัวข้อมูลเสมอ */
export const GRID = "#e4e4e7";

/**
 * จำนวนชิ้นสูงสุดของกราฟวงกลม ก่อนยุบที่เหลือเป็น "อื่น ๆ"
 *
 * ⚠️ เท่ากับจำนวนสีที่มี ห้ามเพิ่มโดยไม่เพิ่มสีที่ผ่านการตรวจแล้ว
 *
 * ลองหาสีที่ห้าแล้วหลายตัว (ม่วง เหลืองเข้ม แดงอิฐ น้ำตาล ฟ้าคราม)
 * ไม่มีตัวไหนแยกจากสี่สีเดิมได้ถึงเกณฑ์เมื่อตรวจแบบทุกคู่ — วงกลมที่มี
 * ห้าชิ้นจึงมีอย่างน้อยหนึ่งคู่ที่คนสายตาปกติแยกไม่ออก ซึ่งแย่กว่าการ
 * ยุบเป็น "อื่น ๆ" ที่ยังบอกยอดรวมได้ตรงไปตรงมา
 */
export const MAX_SLICES = SERIES.length;

/**
 * สีของชิ้น "อื่น ๆ"
 *
 * เทากลาง ๆ โดยตั้งใจ — มันไม่ใช่ตัวตนหนึ่งเดียว แต่เป็นเศษที่เหลือ
 * ถ้าให้สีสดเท่าชิ้นอื่นจะถูกอ่านว่าเป็นหมวดหนึ่งที่มีความหมายในตัว
 */
export const RESIDUAL = "#a1a1aa";
